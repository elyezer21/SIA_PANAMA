# Copia este archivo a .env y ajusta los valores antes de correr docker-compose

GRAFANA_ADMIN_PASSWORD=cambia-esto-en-produccion

# Usado por el frontend en tiempo de build (Vite)
VITE_API_URL=http://localhost:8000
